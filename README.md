# Schapentracker — Minimal Static Version

This is a minimal static app to manage paddocks and sheep locally in the browser.

Run: open `index.html` in a browser. Data is stored in `localStorage` under `schapentracker:data`.
